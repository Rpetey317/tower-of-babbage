# M1-15: clean-clone quick start and documentation audit

- Reworked the README quick start to install dependencies, start only Postgres,
  use the mock provider, sign in to the admin panel, and open the audience view.
- Fixed the docs index, the documented Biome command, and the shared provider
  concurrency description, stale live glossary wording, and the speaker
  stability claim. Removed links to overlay screenshots that are not present
  in the repository.
- Audit pass over every Markdown file in the repo:
  - `docs/decisions.md`: the speaker-labels ADR duplicated the ADR-015 number;
    renumbered to ADR-016 (reference in its change summary updated too).
  - `AGENTS.md`: removed the stale "until M0 lands" note; M0 targets exist.
  - `docs/stack.md`: the `apps/web/src` sketch listed a nonexistent
    `server/events/ingest-token.ts`; corrected to `bus.ts`, `state.ts`,
    `watchdog.ts`, and named the ingest token under `lib/contract/` plus the
    seed and cues files.
  - `apps/web/README.md`: compose step now copies `infra/.env` first, matching
    the root quick start.
- Checked local Markdown links and every script/binary path referenced by the
  docs; all resolve. `scripts/bench-latency.sh` (referenced by testing.md and
  stack.md) is in-flight M2-04 work, so those references stay.
- `docs/benchmark.md` intentionally left absent while the user's benchmark run
  is in progress.
- Added `docs/images/quickstart/audience-captions.png` and
  `admin-dashboard.png`, embedded at the top of the README.

## Verification (clean clone)

Clone: `git clone` of the repo to `/tmp/tob-m115-verify` on this branch, then
the README verbatim:

- `pnpm install` — done in 2.5 s (warm store).
- `cp infra/.env.example infra/.env` + `docker compose --env-file infra/.env
  -f infra/compose.yml --profile infra up -d postgres` — Postgres running
  (reused the existing healthy container; same compose project name).
- `cp apps/web/.env.example apps/web/.env`, `make db-push` ("Changes applied"),
  `pnpm --dir apps/web db:seed` — idempotent; the shared dev DB already had
  `demo-en`/`demo-es` and the seed added the missing `demo-video`.
- `cp services/pipeline/.env.example ...`, `set -a; . ...; set +a`,
  `make web` (Ready on :3000), `make pipeline` (`provider:"mock"` on :8090).
- `GET /api/health` → `{"status":"ok"}`. Authenticated with a `tob_admin`
  cookie minted from `AUTH_SECRET` per `admin-cookie.ts` (the same check the
  login form and middleware perform), started `demo-en` through
  `admin.sessions.start` → `running`; `/s/demo-en` served mock Spanish captions
  and speaker labels; session drained to `idle` at EOF as designed (ADR-013).
- Screenshots: real renders at 1440x900 from headless Chromium (CDP, no extra
  deps) against the verified clone: audience `/s/demo-en?hl=en` while
  `running`, admin `/admin?hl=en` with the signed cookie.
